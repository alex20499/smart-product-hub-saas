# 贾维斯的电商 SaaS 开发协议

## 1. 核心架构：动态 JSONB 模式
- 所有的动态品类参数（Specs）必须存入 `attributes` (JSONB) 字段。
- 严禁为每个品类创建不同的表，必须使用统一的 `products` 表配合 `category_templates` 表。

## 2. 0 基础防御性编程规范
- **禁止白屏**：所有数据读取必须使用 `?.` (Optional Chaining) 并提供兜底默认值 (e.g., `data?.title || "未命名"`)。
- **错误捕获**：所有的数据库请求必须包裹在 try-catch 中，严禁静默崩溃。
- **数据校验**：所有录入操作必须通过 Zod 进行 Schema 验证，拦截脏数据进入数据库。

## 3. 协作模式
- 我是 0 基础开发者，修改代码前请先解释逻辑。
- 优先生成 SQL 脚本让我去 Supabase 运行，确认无误后再修改代码。